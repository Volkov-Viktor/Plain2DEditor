#pragma once

// Включение заголовка SDKDDKVer.h задаёт использование самой новой доступной версии платформы Windows.
// Если требуется собрать приложение для более старой версии Windows, сначала подключите WinSDKVer.h и
// задайте макрос _WIN32_WINNT для целевой платформы — перед включением SDKDDKVer.h.

#include <SDKDDKVer.h>
